Import('rtconfig')
from building import *

cwd     = GetCurrentDir()
src	= Glob('*.c')


CPPPATH = [cwd]

group = DefineGroup('gdb', src, depend = [''], CPPPATH = CPPPATH)

Return('group')

group = DefineGroup('gdb', src, depend = ['RT_USING_GDB'], CPPPATH = CPPPATH)

Return('group')
